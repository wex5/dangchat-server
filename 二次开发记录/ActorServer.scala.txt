actor-server-sdk\src\main\scala\im\actor\server\ActorServer.scala

  def start(): ActorServer = {
    val configPath = ActorEnv.getAbsolutePath("conf/server.conf")
    //解决在bin文件夹下启动actor不能找到server.conf的问题  by Lining  20160526
    /*
    if (Files.exists(configPath)) {
      System.setProperty("config.file", configPath.toString)
    } 
    */
    if (Files.exists(configPath)) {
      System.setProperty("config.file", configPath.toString)
    }     
    else {
      System.setProperty("config.file", ActorEnv.getAbsolutePath("../conf/server.conf").toString)
    }